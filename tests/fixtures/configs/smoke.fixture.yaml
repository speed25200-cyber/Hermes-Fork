# Configuration de smoke test : instruments synthétiques, univers minimal différent. fixture_only=true
# la rend IMPOSSIBLE à charger hors PAPER/RESEARCH.
extends: ../../../configs/base.yaml
project:
  mode: PAPER
  fixture_only: true
account:
  scope: smoke-fixture
  paper_initial_equity_usdt: "10000"
universe:
  target_size: 3
  minimum_eligible: 2
  maximum_eligible: 5
  minimum_history_days: 0
  min_notional_volume_24h_usdt: "0"
  min_depth_notional_usdt: "0"
