# Déploiement de Hermes sur le VPS par SSH depuis un runner GitHub.
# Secrets utilisés (Settings → Secrets and variables → Actions) : VPS_PASSWORD, et pour les modes
# demo/live OKX_API_KEY, OKX_API_SECRET, OKX_API_PASSPHRASE ; optionnels HERMES_TELEGRAM_TOKEN/CHAT.
# Les secrets transitent par l'entrée standard de ssh et ne sont jamais imprimés. L'entraînement sur runner, qui
# exécute des dépendances téléchargées, tourne dans un job sans aucun secret et n'en sort qu'un modèle (donnée sans
# code) ; le job de déploiement, seul à recevoir les secrets, n'exécute aucune dépendance téléchargée.
name: Deploy

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "paper (simulé, sans clé) | demo (compte démo OKX) | live (argent réel)"
        default: "paper"
        type: choice
        options: [paper, demo, live]
      train_on_runner:
        description: "Entraîner le modèle sur ce runner GitHub (≈ 1 h 30) puis l'installer comme champion : un petit VPS n'a pas la mémoire pour entraîner"
        type: boolean
        default: true
      model_config:
        description: "Configuration à entraîner (configs/<nom>.yaml)"
        default: "research_30m_xl_lb_sres"
      model_run:
        description: "À la place : ID d'un run « Research » de CE dépôt dont installer le modèle"
        default: ""
      train:
        description: "Entraîner sur le VPS après l'installation (ignoré sous 12 Go de mémoire)"
        type: boolean
        default: false
      retire_old:
        description: "EFFACER les anciens systèmes (okxq : Docker, base, /opt/okxq ; ancien Node : /root/hermes, services) — refusé si une clé OKX y est posée"
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  train:
    if: ${{ inputs.train_on_runner && inputs.model_run == '' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 330
    env:
      CONF: ${{ inputs.model_config }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Cache des archives
        uses: actions/cache@v4
        with:
          path: |
            data/parsed
            data/venue
          key: binance-parsed-${{ inputs.model_config }}-${{ github.run_id }}
          restore-keys: |
            binance-parsed-${{ inputs.model_config }}-
            binance-parsed-
      - name: Entraînement sur runner
        shell: bash
        run: |
          [[ "$CONF" =~ ^[a-z0-9_]+$ && -f "configs/$CONF.yaml" ]] || { echo "::error::configuration inconnue : $CONF"; exit 1; }
          if [ -f requirements.lock ]; then
            pip install -q --require-hashes -r requirements.lock && pip install -q --no-deps -e .
          else
            pip install -q -e .
          fi
          hermes research run -c "configs/$CONF.yaml" --out "out/$CONF" --n-null 24 2>&1 | tee train.log | tail -60
          [ -f "out/$CONF/model/bundle.json" ] || { echo "::error::pas de modèle produit"; exit 1; }
      - name: Rapport
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: deploy-report-${{ github.run_id }}
          path: |
            out/${{ inputs.model_config }}/REPORT.md
            out/${{ inputs.model_config }}/report.json
            train.log
          retention-days: 90
      - name: Modèle
        uses: actions/upload-artifact@v4
        with:
          name: model-${{ github.run_id }}
          path: out/${{ inputs.model_config }}/model
          retention-days: 90

  deploy:
    needs: train
    if: ${{ always() && (needs.train.result == 'success' || needs.train.result == 'skipped') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    env:
      VPS: ${{ vars.VPS_HOST || '178.104.191.79' }}
      SSHOPTS: -o StrictHostKeyChecking=accept-new -o ConnectTimeout=20 -o ServerAliveInterval=30
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Outils SSH
        run: sudo apt-get update -qq && sudo apt-get install -y -qq sshpass rsync
      - name: Authentification
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: |
          [ -n "$SSHPASS" ] || { echo "::error::secret VPS_PASSWORD absent"; exit 1; }
          sshpass -e ssh $SSHOPTS "root@$VPS" 'echo connecté à $(hostname)'
      - name: Retirer les anciens moteurs
        if: ${{ inputs.retire_old }}
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: sshpass -e ssh $SSHOPTS "root@$VPS" bash -s < deploy/retire_old.sh
      - name: Copier le code
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: |
          sshpass -e ssh $SSHOPTS "root@$VPS" 'mkdir -p /opt/hermes'
          git rev-parse HEAD > REVISION  # le réentraînement mensuel (workflow Retrain) entraîne avec ce code
          sshpass -e rsync -az --delete -e "ssh $SSHOPTS" \
            --exclude /.git --exclude /data/ --exclude /state/ --exclude /artifacts/ --exclude /reports/auto/ \
            --exclude /.venv/ ./ "root@$VPS:/opt/hermes/"
      - name: Installer et démarrer
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
          MODE: ${{ inputs.mode }}
          K1: ${{ secrets.OKX_API_KEY }}
          K2: ${{ secrets.OKX_API_SECRET }}
          K3: ${{ secrets.OKX_API_PASSPHRASE }}
          T1: ${{ secrets.HERMES_TELEGRAM_TOKEN }}
          T2: ${{ secrets.HERMES_TELEGRAM_CHAT }}
          D1: ${{ secrets.HERMES_DASHBOARD_TOKEN }}
        run: |
          if [ "$MODE" != "paper" ] && { [ -z "$K1" ] || [ -z "$K2" ] || [ -z "$K3" ]; }; then
            echo "::error::mode $MODE : secrets OKX_API_KEY / OKX_API_SECRET / OKX_API_PASSPHRASE requis"; exit 1
          fi
          {
            [ -n "$K1" ] && printf 'OKX_API_KEY=%s\nOKX_API_SECRET=%s\nOKX_API_PASSPHRASE=%s\n' "$K1" "$K2" "$K3"
            [ -n "$T1" ] && printf 'HERMES_TELEGRAM_TOKEN=%s\nHERMES_TELEGRAM_CHAT=%s\n' "$T1" "$T2"
            [ -n "$D1" ] && printf 'HERMES_DASHBOARD_TOKEN=%s\n' "$D1"
            true
          } | sshpass -e ssh $SSHOPTS "root@$VPS" "bash /opt/hermes/deploy/install.sh $MODE"
      - name: Modèle entraîné sur runner
        if: ${{ inputs.train_on_runner && inputs.model_run == '' }}
        uses: actions/download-artifact@v4
        with:
          name: model-${{ github.run_id }}
          path: out/model
      - name: Modèle à installer
        if: ${{ inputs.train_on_runner && inputs.model_run == '' }}
        run: |
          [ -f out/model/bundle.json ] || { echo "::error::modèle absent de l'artefact"; exit 1; }
          echo "MODEL_DIR=out/model" >> "$GITHUB_ENV"
      - name: Modèle d'un run Research
        if: ${{ inputs.model_run != '' }}
        env:
          RUN: ${{ inputs.model_run }}
          CONF: ${{ inputs.model_config }}
          GH_TOKEN: ${{ github.token }}
        run: |
          [[ "$RUN" =~ ^[0-9]+$ && "$CONF" =~ ^[a-z0-9_]+$ ]] || { echo "::error::entrées model_run / model_config invalides"; exit 1; }
          NAME="research-$CONF-$RUN"
          API="https://api.github.com/repos/$GITHUB_REPOSITORY/actions"
          ID=$(curl -fsSL -H "Authorization: Bearer $GH_TOKEN" "$API/runs/$RUN/artifacts?per_page=100" \
            | jq -r --arg n "$NAME" '.artifacts[] | select(.name == $n and (.expired | not)) | .id' | head -1)
          [ -n "$ID" ] || { echo "::error::artefact $NAME introuvable (ou expiré)"; exit 1; }
          curl -fsSL -H "Authorization: Bearer $GH_TOKEN" "$API/artifacts/$ID/zip" -o model.zip
          unzip -q model.zip -d art
          B=$(find art -name bundle.json -path '*/model/*' | head -1)
          [ -n "$B" ] || { echo "::error::pas de modèle (model/bundle.json) dans $NAME"; exit 1; }
          echo "MODEL_DIR=$(dirname "$B")" >> "$GITHUB_ENV"
      - name: Installer le modèle et démarrer le moteur
        if: ${{ env.MODEL_DIR != '' }}
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
          MODE: ${{ inputs.mode }}
          CONF: ${{ inputs.model_config }}
        run: |
          # La configuration du champion : le réentraînement mensuel (workflow Retrain) la reprend.
          [[ "$CONF" =~ ^[a-z0-9_]+$ && -f "configs/$CONF.yaml" ]] || { echo "::error::configuration inconnue : $CONF"; exit 1; }
          sshpass -e ssh $SSHOPTS "root@$VPS" 'rm -rf /opt/hermes/artifacts/incoming && mkdir -p /opt/hermes/artifacts/incoming'
          sshpass -e rsync -az -e "ssh $SSHOPTS" "$MODEL_DIR/" "root@$VPS:/opt/hermes/artifacts/incoming/"
          sshpass -e ssh $SSHOPTS "root@$VPS" "cd /opt/hermes && chown -R hermes:hermes artifacts \
            && runuser -u hermes -- .venv/bin/hermes model install artifacts/incoming --to artifacts/models/champion \
            && echo configs/$CONF.yaml > /etc/hermes/research_config \
            && systemctl enable hermes@$MODE >/dev/null && systemctl restart hermes@$MODE && echo hermes@$MODE démarré"

      - name: Entraînement initial (arrière-plan)
        if: ${{ inputs.train }}
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
          MODE: ${{ inputs.mode }}
        run: |
          # retrain.sh démarre le moteur du mode choisi dès que le premier champion est installé.
          sshpass -e ssh $SSHOPTS "root@$VPS" "systemctl start --no-block hermes-retrain.service && echo entraînement lancé"
      - name: État
        env:
          SSHPASS: ${{ secrets.VPS_PASSWORD }}
        run: sshpass -e ssh $SSHOPTS "root@$VPS" bash -s < deploy/status.sh
