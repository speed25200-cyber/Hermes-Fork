[Unit]
Description=Hermes read-only dashboard (%i)
After=network-online.target

[Service]
Type=simple
User=hermes
Group=hermes
WorkingDirectory=/opt/hermes
# Port 8899 (celui qu'ouvrait l'ancien système, effacé par retire_old.sh) ; HERMES_DASHBOARD_PORT le remplace.
Environment=HERMES_DASHBOARD_PORT=8899
EnvironmentFile=/etc/hermes/hermes.env
Environment=PYTHONDONTWRITEBYTECODE=1
# Écoute publique seulement si HERMES_DASHBOARD_TOKEN est défini (sinon le service refuse de démarrer).
# Un seul tableau de bord pour tous les modes (onglets papier / démo / réel) : il lit la racine des états.
ExecStart=/opt/hermes/.venv/bin/hermes live dashboard --state state --host 0.0.0.0 --port ${HERMES_DASHBOARD_PORT}
Restart=always
RestartSec=10
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
